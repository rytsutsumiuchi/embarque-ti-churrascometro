import { PrecoMinimoDirective } from './preco-minimo.directive';

describe('PrecoMinimoDirective', () => {
  it('should create an instance', () => {
    const directive = new PrecoMinimoDirective();
    expect(directive).toBeTruthy();
  });
});
